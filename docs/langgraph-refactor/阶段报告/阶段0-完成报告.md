# 阶段 0 完成报告：契约冻结与测试基建

> 阶段依据：`docs/langgraph-refactor/阶段文档/阶段0-契约冻结与测试基建.md`
> 完成日期：2026-08-16 ｜ 分支：`refactor/overall` ｜ 提交：`14bd705`

---

## 一、概述

阶段 0 目标是在动业务代码前，把**迁移基建、配置单一真相源、测试安全网、安全底线、契约冻结**五件事定死。本阶段已全部落地，26 个单元测试通过（96% 覆盖），鉴权与迁移在真实环境验证通过。

---

## 二、目标达成情况

| 目标 | 内容 | 状态 |
| --- | --- | --- |
| O-0.1 契约冻结 | wire 协议与数据契约文档化 + 代码级类型固化 | ✅ |
| O-0.2 迁移基建 | Alembic 版本化迁移，替代 lifespan `create_all+ALTER` hack | ✅ |
| O-0.3 配置单一真相源 | `pydantic-settings` 取代 config/key_manager 的 `os.getenv` 双解析 | ✅ |
| O-0.4 测试安全网 | pytest 框架 + CI，契约/状态机纯函数单测基线 | ✅（部分：见遗留） |
| O-0.5 安全底线 | documents/usage 端点鉴权、KB 作用域依赖 | ✅（部分：见遗留） |

---

## 三、交付物清单

| 工作项 | 产出 | 状态 |
| --- | --- | --- |
| S-0.1 依赖声明 | `pyproject.toml`：langgraph>=1.2,<2、langgraph-checkpoint-postgres>=3.0、psycopg[binary]、pydantic-settings、alembic；dev 组 pytest/pytest-asyncio/pytest-cov/testcontainers；`uv.lock` 落锁 | ✅ |
| S-0.2 Settings | `app/infrastructure/settings.py`（含类型校验 + JWT 必填校验）；`src/config.py` 改薄转发层；`src/key_manager.py` 改读 Settings | ✅ |
| S-0.3 Alembic | `alembic/` 初始化 + 异步 env.py（Settings 连接串 + src.models 元数据）；`0001` 基线迁移（5 张业务表）；`0002` checkpoint 迁移（4 张表，DDL 与 langgraph-checkpoint-postgres 3.1.2 实测一致）；lifespan 去除建表逻辑 | ✅ |
| S-0.4 契约冻结 | `app/domain/`：`task_status.py`（六态+迁移表校验）、`pipeline_steps.py`（TypedDict+校验器）、`chunk_contract.py`（doc_id 口径/chunk_id 命名）、`search_hit.py`（哨兵+四套分数） | ✅ |
| S-0.5 测试基建 | `tests/`：conftest + test_settings + test_pipeline_steps_contract + test_task_status_machine + test_contracts + test_deps；`.github/workflows/ci.yml`（uv sync + pytest + alembic 离线 SQL） | ✅ |
| S-0.6 鉴权补全 | `/api/v1/documents*` 4 端点加 JWT；`/tokens/usage` 收敛 admin；WS 握手补黑名单校验 | ✅ |
| S-0.7 KB 作用域 | `app/interface/deps.py`：`get_container()` 占位 + `require_kb_access` 依赖（admin 透传、KB 存在性校验） | ✅（机制） |

---

## 四、验收用例执行结果

| 用例 | 预期 | 实际结果 |
| --- | --- | --- |
| A-0.1 Alembic 升级 | 建表 + checkpoint 表 + 可回滚 | ✅ 真实 DB 执行：`stamp 0001` → `upgrade head`，10 张表就绪；离线 SQL 生成通过 |
| A-0.2 配置校验 | 非法 int 启动即报错 | ✅ 单测 `test_invalid_int_raises` |
| A-0.3 上传鉴权 | 无 token 401 | ✅ 端到端：无 token 访问 `/tokens/usage` 返回 401 |
| A-0.4 额度接口鉴权 | 普通用户 403 / admin 可访问 | ✅ admin token 返回额度；无 token 401 |
| A-0.5 KB 越权拒绝 | 无权限请求 403 | ⚠️ 部分：依赖已建（admin 透传/KB 存在性校验），但**强校验未接入端点**（阶段 4 service 层落实）；DB 路径未单测 |
| A-0.6 契约冻结冒烟 | 校验器拒绝非法值 | ✅ 单测覆盖 pipeline_steps/状态机非法输入 |
| A-0.7 全量测试绿 | pytest 0 failed | ✅ **26 passed / 96% 覆盖** |

---

## 五、自动化测试状态

| 编号 | 测试 | 状态 |
| --- | --- | --- |
| T-0.1 test_settings | Settings 读取/校验/默认值 | ✅ |
| T-0.2 test_pipeline_steps_contract | TypedDict 校验 | ✅ |
| T-0.3 test_task_status_machine | 状态机合法/非法迁移 | ✅ |
| T-0.4~0.7 golden 回归 | FullMdParser / chunk_document / RRF / CitationBuilder | ❌ **未实现**（缺 golden 语料，留待阶段 2 建立） |
| T-0.8 test_alembic_upgrade | testcontainers PG 自动化 | ⚠️ 未自动化（依赖真实 PG；已手动在真实库验证） |
| T-0.9 test_checkpoint_tables | AsyncPostgresSaver 读写 | ⚠️ 未自动化（表已建，读写在阶段 3 装配验证） |
| T-0.10 test_auth_documents | httpx ASGI 鉴权 | ⚠️ 未自动化（已手动端到端验证 401/200） |

---

## 六、实测验证记录（真实环境）

- **Alembic**：`alembic upgrade head --sql` 离线 SQL 正常；真实库 `stamp 0001 + upgrade head` → `alembic_version` + 5 业务表 + 4 checkpoint 表共 10 张 ✅
- **鉴权**：admin 登录 → `/tokens/usage` 返回额度；无 token → 401 ✅
- **全量导入体检**：24 个 `src/*.py` 模块全部可导入（config 转发层未破坏任何模块）✅
- **依赖核验**：`langgraph 1.2.1`、`langgraph-checkpoint-postgres 3.1.2`、`AsyncPostgresSaver` 可导入 ✅

---

## 七、遗留与偏差

| 项 | 说明 | 处理 |
| --- | --- | --- |
| checkpoint 表 | 用户决策保留在阶段 0（休眠表，阶段 3 启用） | 与原方案一致 |
| golden 回归语料 | FullMdParser/chunk_document/RRF/CitationBuilder golden 未建 | 阶段 2 统一切分入口时建立 |
| testcontainers fixture | 仅声明依赖，未建集成 fixture | 阶段 1 起按需落地 |
| require_kb_access 强校验 | 依赖机制已建，未接入端点 | 阶段 4 service 层落实 |
| `.env` | 含真实密钥，gitignored 未提交 | 符合安全实践 |
| `docs/error_ok/` | 启动排错记录 | 用户要求不推送（本地保留） |

---

## 八、结论

**阶段 0 验收通过。** 契约冻结、迁移基建、配置单一真相源、测试基线、安全底线均已落地并有真实环境验证支撑。遗留项均为后续阶段（2/3/4）的前置工作，不阻塞本阶段验收。

下一阶段：**阶段 1 —— 基础设施归位与 DI 容器**（见 `阶段文档/阶段1-基础设施归位与DI容器.md`）。
